# web-admin Instructions

- Frontend is not the security source of truth.
- Use typed API clients.
- Use URL-synced tabs for major workspaces.
- No browser `alert` or `confirm` APIs.
- Use accessible dialogs.
- Do not use UUIDs as user-facing primary labels.
- Follow the existing design language.
- Route visibility and backend enforcement must both exist.
