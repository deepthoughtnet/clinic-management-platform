import CarePilotPlaceholderView from "../shared/CarePilotPlaceholderView";

export default function RemindersPage() {
  return <CarePilotPlaceholderView moduleName="Reminders" summary="Configure automated reminders for appointments, follow-ups, and due clinical events with audit-friendly traceability." />;
}
