import CarePilotPlaceholderView from "../shared/CarePilotPlaceholderView";

export default function LeadsPage() {
  return <CarePilotPlaceholderView moduleName="Leads" summary="Capture, qualify, and route inbound leads into clinic workflows with assignment and lifecycle visibility." />;
}
