import CarePilotPlaceholderView from "../shared/CarePilotPlaceholderView";

export default function MessagingPage() {
  return <CarePilotPlaceholderView moduleName="Messaging" summary="Manage structured patient messaging templates, channels, and delivery policy in one operational inbox." />;
}
